# Режиссёрский отчёт: auto-20260824-051301

## Раскадровка

1. Хук: составное число 8051 появляется как цель; случайная прогулка раскалывает его на 83 и 97; термин метода появляется в заголовке.
2. Сцена «Цикл вместо перебора»: функция превращает остатки в последовательность; на слове «Черепаха» один бегунок и два бегунка расходятся и замыкаются в скрытый цикл.
3. Сцена «Пример на 8051»: лента остатков показывает 2 → 5 → 26 → 677; на «На» раскрывается вычислительный удар с разностью 194 и НОД 97.
4. Сцена «Почему НОД всё раскрывает»: две скорости сходятся; на «Разность» появляется 194, затем 97 и итоговое разложение; на «Нужно» остаётся подпись про порядок корня из множителя и малую память.
5. Аутро: три коротких вывода и мостик к следующему математическому трюку.

## Gap-скан

| Действие из реплики | Лучший существующий визуал | Оценка | Решение |
|---|---|---:|---|
| Случайная прогулка раскалывает 8051 | нет визуала Полларда rho | 1 | кузница `pollard-rho` |
| Черепаха идёт на один шаг, заяц на два | нет визуала поиска цикла двумя скоростями | 1 | фаза `collision` |
| Остатки 2, 5, 26, 677 дают вычислимый пример | `collision-compare`, но он про хеш-коллизии | 2 | фаза `walk` |
| Разность и НОД раскрывают множитель | `collision-compare`, семантика не совпадает | 2 | фаза `factor` |
| 8051 раскладывается в 83 × 97 | нет визуала факторизации | 2 | фаза `factor` |

До кузницы: 5 действий, 5 оценок ≤3. Новых визуалов: **1**, `pollard-rho`, документирован в `.claude/skills/animator/catalog.md`; после кузницы действий с оценкой ≤3 не осталось.

## Визуал и синхронизация

`pollard-rho` зарегистрирован в типах, схеме и `StoryScene`. Фазы:

- `walk`: функция «квадрат плюс один», цепочка остатков;
- `collision`: черепаха и заяц, один/два шага, найденный цикл;
- `factor`: `|677 − 871| = 194`, `НОД(194, 8051) = 97`, `8051 = 83 × 97`, порядок корня из множителя.

Якоря: сцена 1 — нет (первый бит) и `Черепаха`; сцена 2 — нет (первый бит) и `На`; сцена 3 — нет, `Разность`, `Нужно`. Хук и аутро не имеют битовых якорей.

## Проверки

- `npx tsc --noEmit` — OK.
- Forge Preview для `pollard-rho` — `/tmp/pollard-rho-forge.png`, `/tmp/pollard-rho-collision.png`, `/tmp/pollard-rho-factor.png`.
- `python3 tools/validate.py episodes/auto-20260824-051301.json` — OK: 5 сцен, ~139 слов.
