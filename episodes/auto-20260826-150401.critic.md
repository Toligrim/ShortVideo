# Критика auto-20260826-150401

## Предварительный gate

`cd video && node scripts/check-overlaps.cjs auto-20260826-150401` — exit `0`; 12/12 кадров, `0 overlap(s)`.

## Ручной осмотр

- `0a`, кадр 133: хук крупный и контрастный; заголовок и подзаголовок полностью в safe-зоне, смысловой текст не касается субтитров.
- `0b`, кадр 284: хук стабилен после входной анимации; строки не обрезаны, пустого центра нет.
- `1a`, кадр 482: шесть отсортированных ID и разбиение «верх/низ» читаются; подпись и караоке разнесены.
- `1b`, кадр 612: второй beat показывает ту же операцию без наложений; карточки и двоичные части остаются различимыми, субтитр не перекрывает смысловую область.
- `2a`, кадр 796: две битовые строки, цветовое различение и стрелка читаются; визуал буквально соответствует младшим/верхним частям.
- `2b`, кадр 925: после переноса якоря на «Верхние» обе строки полностью вошли и ярко читаются; подпись объясняет единицы и нули.
- `3a`, кадр 1105: заголовок, формула и нижняя шкала помещаются в safe-зоне; формула не пересекается с караоке.
- `3b`, кадр 1231: формула и подпись к минимуму остаются читаемыми на импакте; крупный субтитр отделён снизу.
- `4a`, кадр 1411: выбранная единица, обе битовые строки и стрелка дают понятную реконструкцию; зелёный результат не закрывает текст.
- `4b`, кадр 1538: состояние «выбираем единицу» и весь список различимы; нижний субтитр находится отдельно.
- `5a`, кадр 1737: outro с тремя пунктами читаем, не обрезан и не занимает верхнюю/нижнюю UI-зоны.
- `5b`, кадр 1886: CTA полностью виден выше safeBottom; bullets, CTA и караоке не пересекаются.

## Вердикт

**ПРИНЯТО** — все 12 кадров прошли ручной чек; новый визуал объясняет разбиение, унарный слой, оценку размера и доступ без распаковки, типографика читаема.
