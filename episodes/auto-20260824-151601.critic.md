# Critic review — auto-20260824-151601 (финальный круг, после исправлений)

ПРИНЯТО — все четыре правки прошлого круга подтверждены на живых кадрах: чипы
несут текст, подписи 5%/95% разведены без наездов, якорь «Остальные» переключает
фазу вовремя, limit-лейблы с числами из озвучки на месте и в кадре.

## Проверка исправлений (кадры отрендерены и осмотрены как изображения)
1. **Текст всех Chip виден** — исправлено. «1 нить», «12 нитей», «5% — только в
   одну нитку, не размножить» (1-a, 2-a), «(1 − p) — серийная доля»,
   «p / n — параллельная, делённая на ядра» (1-b, 2-b), «предел ≈ 20× (1 / 0.05)»,
   «100 ядер ≈ 16.8×», «1000 ядер ≈ 19.6×» (3-a, 3-b) — всё читается, пустых
   пилюль нет.
2. **«5% серийная»** — подпись вынесена под узкий сегмент (1-a, 2-a): видна
   целиком, с параллельным блоком и с «95% параллельной» не пересекается.
3. **«95% параллельная»** — лежит ниже нижней границы блока, на border не
   наезжает (1-a, 2-a).
4. **Якорь «Остальные»** — ранней смены фазы больше нет: кадр 2-a (лок.
   ~3.97s, «…можно распараллелить») ещё в workers-фазе со сплит-баром 5/95 и
   пульсацией серийного сегмента; формула появляется только на 2-b (~8.1s).

## Чек-лист по всем 10 кадрам
- **Читаемость**: весь текст крупный, контрастный, не обрезан. Формульная панель
  переносит «S = 1 / ((1 − p) + p / n)» на две строки — читается свободно, ничего
  не вылезает (косметика, не блокер). Чип «1000 ядер ≈ 19.6×» заканчивается в
  ~5–10px от правого края — впритык, но скруглённый край и текст полностью в
  кадре (проверено зум-кропом 3-a и 3-b).
- **Перекрытия**: элементов друг на друге нет; пунктирная линия предела и кривая
  проходят под полупрозрачными чипами, текст поверх читается. Субтитры с
  графикой не конфликтуют.
- **Safe-зоны**: верхние 150px пусты во всех кадрах (заголовки с y≈270); в нижних
  420px только субтитры-капшены (y≈1550) — разрешено; CTA-пилюля (4-b) на
  y≈1140–1270, выше зоны перекрытия.
- **Не пусто**: хук-тайтл, сплит-бар с воркерами, формульная панель, график
  насыщения, буллиты+CTA — в каждом кадре есть смысловой центр.
- **Семантика**: 0-a/0-b — хук про тысячу ядер; 1-a — разделение работы;
  1-b/2-b — формула с подставленными 0.05/0.95; 3-a — «Всего двадцать» и жёлтый
  «≈20×» с пунктиром; 3-b — «…и восьми десятых» и зелёный «100 ядер ≈ 16.8×»;
  4-b — CTA ровно на «Подписывайся». Цвета консистентны: red = серийное узкое
  место, cyan = параллельное, green = 100 ядер, yellow = предел, purple = 1000 ядер.

## Проверенные кадры (все просмотрены как изображения)
- qc-…-0-a (S0 @127, «…задачей за час») — ок
- qc-…-0-b (S0 @272, «за три секунды? Ан нет») — ок
- qc-…-1-a (S1 @518, workers, сплит-бар) — ок
- qc-…-1-b (S1 @708, «…параллельной доли…», formula) — ок
- qc-…-2-a (S2 @926, «…можно распараллелить», workers) — ок, фаза не перескочила
- qc-…-2-b (S2 @1051, «в одну нитку», formula) — ок
- qc-…-3-a (S3 @1272, «двадцать. Всего», limit) — ок
- qc-…-3-b (S3 @1446, «…и восьми десятых», limit) — ок
- qc-…-4-a (S4 @1703, буллиты) — ок
- qc-…-4-b (S4 @1883, «Подписывайся…», CTA виден) — ок

## Неблокирующие заметки (в прод — по желанию)
- Формулу в панели можно уместить в одну строку (уменьшить fontSize 60 → ~52
  или расширить minWidth) — сейчас перенос посреди скобки выглядит чуть грубо.
- Чип «1000 ядер ≈ 19.6×» держится на запасе ~5–10px справа: при замене текста
  не делать его длиннее.
- Сцена 0 — статичный тайтл все 11.3s; для хука допустимо.

PNG-артефакты QC (`video/qc-auto-20260824-151601-*.png`) удалены после осмотра.
Эпизод, код, ассеты не менялись.
