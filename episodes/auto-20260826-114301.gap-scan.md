# Gap-скан: auto-20260826-114301 (пропускающий список)

Драфт: 6 блоков (hook + 4 story + outro). Библиотека до этого этапа уже содержала
новый визуал `skip-list` (5 фаз: compare / coin / levels / search / insert), добавленный
в предыдущей попытке. Gap-скан проверяет, покрывает ли он действия реплики.

Блок 4 драфта («Математика Пьюга… 1/200 млн») намеренно не вошёл в эпизод — его
концепция O(log n) уже показана фазой `levels`, а словесно повторена в hook и outro;
это сэкономило ~28 токенов, чтобы уложиться в лимит 95–150 слов шортса (итог 123).

| # | Действие из реплики | Лучший существующий визуал | Оценка 1–5 |
|---|---|---|---|
| 1 | список O(n) против дерева O(log n) с вращениями (hook) | `skip-list` / `compare` | 5 |
| 2 | на каждом узле монетка, орёл → длинный указатель (блок 2) | `skip-list` / `coin` | 5 |
| 3 | уровни перепрыгивают узлы сверху вниз (блок 2) | `skip-list` / `levels` | 5 |
| 4 | поиск сверху вниз по длинным указателям, как бинарный поиск (блок 3) | `skip-list` / `search` | 5 |
| 5 | вставка: монетка вместо вращений, против splay-дерева (блок 5) | `skip-list` / `insert` + `compare` | 5 |

**Число действий:** 5
**Число оценок ≤3:** 0

Правило «≤3 → в очередь кузницы» не сработало: все действия буквально покрыты
фазами `skip-list` (оценка 5). Новый визуал уже существует (добавлен в предыдущей
попытке и документирован в catalog.md на этом этапе), поэтому сборка эпизода
допустима без создания дополнительных визуалов.

**Новых визуалов на этом этапе:** 0 (визуал `skip-list` уже был; в этом коммите —
только его документация в catalog.md).
