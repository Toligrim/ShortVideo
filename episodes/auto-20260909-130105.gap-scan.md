# Animation-director: auto-20260909-130105

Источник: `episodes/drafts/auto-20260909-130105.draft.json`.
Реплики в episode перенесены дословно, включая `{IPv4|ай пи ви четыре}`, `{IP|ай пи}` и `{NAT/PAT|нат пэт}`. Фактических ошибок в draft не найдено; narration не редактировался.

## Раскадровка по блокам

| Блок | Сущности → объекты | Действия → движение | Ударные слова → onWord | Визуальная сборка |
|---:|---|---|---|---|
| 1 | дом, телефоны, ноутбуки, сайт, публичный адрес, ответ | устройства сходятся к границе; запрос уходит наружу; сайт видит одну точку | первый `browser-click` стартует с кадра 0; `несколько` → `nat-pat-translation:home` | браузер открывает сайт, затем панель дома ведёт пакет через роутер к сайту с одним внешним IPv4 |
| 2 | телефон, private IP, порт, роутер, public endpoint | пакет движется от телефона к сайту; NAT/PAT переписывает пару адреса и порта на границе | `nat-pat-translation:translate` стартует с кадра 0; `роутер` → `title-slam` | три панели «телефон → NAT/PAT → сайт», два endpoint показаны до и после переписи |
| 3 | гардероб, номерок, вещь, строка отображения | номерок летит к строке; строка связывает внешний порт с вещью | `гардероб` → `title-slam`; `NAT/PAT` → `nat-pat-translation:ticket` | короткий title-slam вводит номерок, затем отдельная композиция гардероба и таблицы показывает связь «№ → вещь» |
| 4 | сайт, публичный порт, таблица, строка, телефон, ответный пакет | ответ возвращается; lookup подсвечивает строку; назначение переписывается к телефону | `таблицу` → `nat-pat-translation:return`; `именно` → `title-slam` | сайт слева, таблица в центре, телефон справа; ответ проходит по public port и выходит на private port нужного телефона |
| 5 | частный/public адрес и порт, таблица, NAT/PAT, трафик, имя сайта, адрес, CTA | финальная связка механизма; переход к следующей теме DNS | outro, story onWord не требуется | штатный `outro` с тремя bullets и мостиком к DNS; narration не повторяется отдельным story-visual |

## Сгруппированные смысловые идеи и gap-скан

Скан намеренно ограничен пятью идеями — по одному смысловому блоку, без дробления на отдельные клики, переписывания и движения пакета. Оценка до кузницы: 5 — буквальное покрытие, 3 — похожая аналогия, 1 — визуал про другое.

| № | Смысловая идея из реплик | Лучший существующий visual | До | Решение | После |
|---:|---|---|---:|---|---:|
| 1 | Несколько домашних устройств одновременно выходят наружу, а внешний сайт видит один публичный IPv4 и должен различать потоки | `diagram` | 3 | `nat-pat-translation:home` добавляет панель локальной сети, общую границу роутера и одну внешнюю точку; пакет явно выходит к сайту | 5 |
| 2 | Запрос начинается с частного IP:port и на границе получает публичные IP:port | `diagram` | 2 | `nat-pat-translation:translate` буквально ставит private endpoint и public endpoint по разные стороны NAT/PAT; пакет пересекает панель переписи | 5 |
| 3 | NAT/PAT — не магическая надпись: отображение можно понять как гардеробный номерок, ведущий к вещи/строке | `origin-check` | 2 | `nat-pat-translation:ticket` показывает летящий номерок и строку «№ → вещь»; это отдельная композиция, а не смена подписи | 5 |
| 4 | Ответ приходит на публичный порт, таблица находит сохранённое отображение и отправляет пакет нужному телефону | `diagram` | 3 | `nat-pat-translation:return` показывает public port → выделенную строку lookup → private port телефона; направление ответа видно движением пакета | 5 |
| 5 | Итог: устройства делят внешний адрес, NAT/PAT не шифрует и не делает анонимным; следующий мостик — DNS | `outro` | 5 | Штатный outro уже рассчитан на выжимку и CTA; новый story-visual здесь не нужен | 5 |

Итого: **5 смысловых идей**, **4 оценки ≤3 до кузницы**, **0 оценок ≤3 после покрытия**. Каждая исходная оценка ≤3 получила новое визуальное покрытие; одна кузница закрывает четыре дефицита четырьмя разными фазами.

## Новый visual

Добавлен один переиспользуемый visual: `nat-pat-translation`.

- Реализация: `video/src/scenes/NatPatTranslationVisual.tsx`.
- Фазы: `home`, `translate`, `ticket`, `return` — всего **4**, то есть ≤6.
- `home` — локальная домашняя группа и один public IPv4; `translate` — private IP:port → public IP:port; `ticket` — гардеробный номерок и строка отображения; `return` — обратный lookup по публичному порту к private endpoint.
- В каждой фазе не более четырёх смысловых групп; дополнительно используется только одна короткая нижняя подпись. Строка narration на canvas не выводится.
- Параметры `privateIp`, `privatePort`, `publicIp`, `publicPort`, `deviceLabel`, `siteLabel` делают visual переиспользуемым для других endpoint-ов.
- Регистрация: `video/src/lib/types.ts`, `schema/scenes.schema.json`, `video/src/scenes/StoryScene.tsx` (timing, SFX, camera и switch).
- Документация и строка «когда использовать»: `.claude/skills/animator/catalog.md`, раздел `nat-pat-translation`.

## Состав episode

`story/browser-click → nat-pat-translation[home] → nat-pat-translation[translate] → title-slam → title-slam → nat-pat-translation[ticket] → nat-pat-translation[return] → title-slam → outro`.

Сцены по смысловым блокам: `story/home → story/translate → story/ticket → story/return → outro`.
Новый visual используется ровно по одной фазе в каждой из четырёх story-сцен; композиция меняется между сценами, а narration остаётся только в karaoke-слое.

## onWord-якоря по сценам

| Сцена | Тип / состав | onWord-якоря в порядке narration |
|---:|---|---|
| 0 | `story`: `browser-click` + `nat-pat-translation/home` | первый `browser-click` — старт сцены; `несколько` → `home` |
| 1 | `story`: `nat-pat-translation/translate` + `title-slam` | первый `translate` — старт сцены; `роутер` → `title-slam` |
| 2 | `story`: `title-slam` + `nat-pat-translation/ticket` | `гардероб` → `title-slam`; `NAT/PAT` → `ticket` |
| 3 | `story`: `nat-pat-translation/return` + `title-slam` | `таблицу` → `return`; `именно` → `title-slam` |
| 4 | `outro` | story onWord не требуется |

Все якоря присутствуют в show-части соответствующей реплики; first beat без onWord намеренно стартует с нулевого кадра сцены. Визуальные подписи — короткие объектные ярлыки (`IP:port`, `№ → вещь`, `lookup`), а не повтор narration.
