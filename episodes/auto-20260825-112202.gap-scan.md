# Gap-scan: auto-20260825-112202

Тема: согласованное хеширование — локальный перенос ключей при изменении состава узлов.

Оценка до кузницы — соответствие лучшего существующего визуала. Оценка после — соответствие нового `consistent-hash-ring`.

| Действие из реплики | Лучший существующий визуал | До | Новый визуал | После |
|---|---|---:|---|---:|
| При добавлении узла modulo меняет адреса почти всех ключей | `hash-table` — один ключ и одна ячейка, без перераскладки | 2 | `modulo` — две схемы рядом, красный перенос | 5 |
| Узлы и ключи раскладываются на круге | `hash-table` — линейная таблица | 2 | `ring` — кольцо, узлы и ключевые точки | 5 |
| Ключ идёт по часовой стрелке до первого узла | `diagram` — стрелка между узлами без правила кольца | 3 | `ring` — пунктирная стрелка к владельцу | 5 |
| Новый узел забирает сектор между соседями | `hash-table` — нет локального диапазона | 1 | `move` — зелёный сектор и узел `+D` | 5 |
| При четырёх равных узлах меняется примерно четверть ключей | `power-of-two-choices` — корзины, не диапазоны кольца | 2 | `move` — бейдж `≈ 25% КЛЮЧЕЙ` | 5 |
| Случайные позиции могут дать одному серверу огромный сектор | `power-of-two-choices` — балансировка без кольца | 2 | `virtual` — несколько точек по окружности | 5 |
| Виртуальные точки выравнивают нагрузку | `power-of-two-choices` — только два выбора корзины | 2 | `virtual` — много позиций одного сервера | 5 |
| Остальные ключи остаются на старых владельцах | `hash-table` — не показывает стабильность при росте N | 1 | `move` — невыделенные точки остаются на кольце | 5 |

Итого: 8 действий, до кузницы 8 оценок ≤3, после кузницы 0 оценок ≤3. Визуал добавлен: 1 — `consistent-hash-ring` с фазами `modulo`, `ring`, `move`, `virtual`. Правило роста выполнено: все низкие соответствия получили новый визуал, нулевых новых визуалов нет.

Состав episode: `hook` → `story(modulo)` → `story(ring)` → `story(move)` → `story(virtual)` → `outro`.

Якоря: s1 — `схеме`, `добавление`; s2 — `узлы`, `Ключ`; s3 — `новый`, `четверть`; s4 — `точки`, `Dynamo`.
