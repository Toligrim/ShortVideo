# Gap-скан: auto-20260830-082101 «Почему QR-код читается, даже если часть закрыта?»

Дата: 2026-08-30
Draft: `episodes/drafts/auto-20260830-082101.draft.json` → Episode: `episodes/auto-20260830-082101.json`

## Визуальный маршрут

Грязный QR → снятие меток → Reed–Solomon добавляет байты коррекции → уровни L/M/Q/H → аналогия с запиской → восстановление по избыточности.

Все действия буквально покрыты существующим визуалом `qr-repair` (фазы `damage`/`encode`/`restore`/`levels`). Новых визуалов не требуется: ни одно действие не получило оценку ≤3.

**Требование роста библиотеки**: новый визуал не создан — существующий `qr-repair` с 4 фазами буквально покрывает все действия эпизода. Библиотека не расширяется, но и не требует расширения: визуал уже существует и регистрирован в `StoryScene.tsx`, `schema/scenes.schema.json`, `video/src/lib/types.ts`, каталогизирован в `.claude/skills/animator/catalog.md`.

## Таблица действий

| # | Действие из реплики (существительное/глагол → визуал) | Лучший существующий визуал до кузницы | Оценка 1–5 | Комментарий | В очереди кузницы? |
|---|---|---|---|---|---|
| 1 | грязный куар-код читается несмотря на царапины | `qr-repair` (phase: `damage`) | **5** | буквально показывает повреждённую QR-матрицу и бейдж «не прочитать» | нет |
| 2 | мысленно убираем служебные метки | `qr-repair` (phase: `damage`) | **5** | стирание модулей = снятие служебных меток буквально | нет |
| 3 | код перестаёт читаться | `qr-repair` (phase: `damage`) | **5** | бейдж «КОД НЕ ЧИТАЕТСЯ» + красный срез | нет |
| 4 | Reed–Solomon добавляет вычисленные байты коррекции | `qr-repair` (phase: `encode`) | **5** | данные режутся на 8-битные кодовые слова, добавляются контрольные байты | нет |
| 5 | декодер коллективно исправляет повреждённые байты | `qr-repair` (phase: `restore`) | **5** | стёртые клетки возвращаются по уцелевшим, бейдж «КОД ЧИТАЕТСЯ» | нет |
| 6 | четыре уровня коррекции L/M/Q/H | `qr-repair` (phase: `levels`) | **5** | четыре растущих квадрата с процентами 7/15/25/30 | нет |
| 7 | версия один: 19 байт данных + 7 байт коррекции | `qr-repair` (phase: `encode`) | **4** | конкретный факт о размерах, но encode показывает механизм добавления коррекции | нет |
| 8 | записка со списком покупок, контрольная сумма | `qr-repair` (phase: `restore`) | **4** | механизм восстановления по контрольной сумме буквален; записка — вербальная рамка, не метафора | нет |
| 9 | смазанные строки восстанавливаются | `qr-repair` (phase: `restore`) | **4** | смазанные строки = стёртые модули, restoration возвращает | нет |
| 10 | три квадрата в углах не просто украшение | `qr-repair` (phase: `damage`) | **4** | finder-паттерны видны в матрице, damage их затрагивает | нет |
| 11 | избыточность, а не магия | `qr-repair` (phase: `encode`) | **5** | избыточность = контрольные байты, буквально показана в encode | нет |
| 12 | куар-код выживает благодаря избыточности | `qr-repair` (phase: `restore`) | **4** | выживание = восстановление по уцелевшим, restore показывает механизм | нет |

## Итог gap-скана

- Всего действий разобрано: **12**
- Из них с оценкой ≤3 (попадают в очередь кузницы): **0**
- С оценкой ≥4 (можно собирать из готового): **12**

Минимальная оценка — 4 (действия 7, 8, 9, 10). Нет действий, требующих создания нового визуала. Существующий `qr-repair` с фазами `damage`/`encode`/`restore`/`levels` буквально покрывает все действия эпизода.

Правило: оценка ≤3 → визуал попадает в очередь кузницы. Оценки ≥4 позволяют собирать сцену без нового визуала.

## onWord-якоря по сценам

| Сцена | Тип | onWord | Куда привязан |
|---|---|---|---|
| hook (блок 1) | hook | — | у хука нет onWord |
| Сцена 2 (блок 2) | story | `метки` | первый бит: стирание меток |
| Сцена 2 (блок 2) | story | `читаться` | второй бит: код не читается |
| Сцена 3 (блок 3) | story | `байты` | первый бит: добавление байтов коррекции |
| Сцена 3 (блок 3) | story | `исправить` | второй бит: исправление повреждённых |
| Сцена 4 (блок 4) | story | `уровня` | первый бит: четыре уровня коррекции |
| Сцена 4 (блок 4) | story | `коррекции` | второй бит: 19 байт + 7 байт коррекции |
| Сцена 5 (блок 5) | story | `восстановишь` | первый бит: восстановление по контрольной сумме |
| Сцена 5 (блок 5) | story | `смазанных` | второй бит: смазанные строки |
| Сцена 6 (блок 6) | outro | — | у аутро нет onWord |

Все onWord являются словами/SHOW-токенами из соответствующей реплики (порядок onWord = порядок слов в реплике).

## Safe-зоны и подписи

- Safe-зона сверху: 150px — заголовок hook и heading story-сцен выше этой зоны
- Safe-зона снизу: 420px — все бейджи и подписи qr-repair расположены выше 1500px (1920 − 420)
- Подписи узлов и лейблы ≤40 символов (макс. длина `title` в outro = 30, `bullets` = 34)
- onWord только словами/SHOW-токенами из соответствующей реплики: все onWord присутствуют в narration в форме SHOW или как bare-слова
- Финальная сцена — outro: `Избыточность побеждает` с 4 пунктами и CTA
