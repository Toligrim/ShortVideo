# Gap-scan — auto-20260826-082201

Режиссура поверх уже откованного визуала `hilbert-curve` (фазы
`grid` / `locality` / `cache`). Драфт: кривая Гильберта как
space-filling обход с локальностью и кэш-независимостью.

## Таблица: действие → лучший существующий визуал → оценка

Легенда оценки (по SKILL/agent): 5 = буквально показывает действие,
3 = похожее (зритель достроит), 1 = про другое.

### Блок 1 — хук (линия через все клетки, спасает кэш)
| Действие (из реплики) | Лучший существующий visual | Оценка |
|---|---|---|
| рисуешь одну линию через каждую клетку | `diagram` (узлы+канал) | 2 |
| линия не отрывается, ни одного пропуска | `counter` | 1 |
| линия помогает не терять кэш | `heap-graph` | 2 |

### Блок 2 — как пятилетке (змейка в саду, сосед рядом)
| Действие | Лучший существующий visual | Оценка |
|---|---|---|
| змейка ползёт по всем грядкам подряд | `rule-110` (ряды клеток) | 3 |
| соседняя клетка всегда рядом | `collision-compare` | 2 |
| шаг короткий и аккуратный | `hamming-word` | 1 |

### Блок 3 — как инженеру (кривая Гильберта, шаг в соседа, локальность)
| Действие | Лучший существующий visual | Оценка |
|---|---|---|
| переходит ровно в соседнюю клетку | `debruijn-cycle` (цикл по клеткам) | 2 |
| рекурсивная склейка сохраняет локальность | `suffix-automaton` | 1 |
| близкие точки остаются близкими | `consistent-hash-ring` | 3 |

### Блок 4 — вау-факт (кэш-независимый, O(log n), матрицы)
| Действие | Лучший существующий visual | Оценка |
|---|---|---|
| кэш-независимый обход | `bloom-bitarray` | 2 |
| координату/индекс считают за O(log n) | `bbp-digit` | 2 |
| ускоряет умножение матриц | `fft-wave` | 2 |

### Блок 5 — выжимка (линия держит данные рядом, спасает кэш)
| Действие | Лучший существующий visual | Оценка |
|---|---|---|
| линия обходит весь квадрат | `diagram` | 2 |
| держит данные рядом | `union-find` | 2 |
| спасая кэш | `heap-graph` | 2 |

## Итог gap-скана
- **Всего действий:** 15 (по 3 из каждого из пяти блоков).
- **Действий с оценкой ≤3:** 15 (все — ни один готовый визуал не
  показывает space-filling кривую и её локальность/кэш-независимость
  буквально; максимум 3 у похожих рядно-клеточных визуалов).
- **Новые visual (имена):** `hilbert-curve`.

## Кузница (результат предыдущего этапа, здесь не повторяется)
Новый visual **`hilbert-curve`** с фазами `grid` / `locality` / `cache`
уже создан (`video/src/scenes/HilbertCurveVisual.tsx`) и зарегистрирован
в `StoryScene.tsx`, `lib/types.ts`, `schema/scenes.schema.json`. Он
закрывает все 15 действий с оценкой 5 — сцена собрана без компромиссов
(ни один бит не стоит на визуале ≤3).

## Итоговое число новых визуалов: N = 1 (`hilbert-curve`)
