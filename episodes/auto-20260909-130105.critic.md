# Критика — auto-20260909-130105

## Вердикт

**ПРИНЯТО — разрешаю render.**

## Контрольные проверки

- `python3 tools/validate.py episodes/auto-20260909-130105.json` — PASS: 5 сцен, ~140 слов.
- `cd video && npx tsc --noEmit` — PASS.
- `cd video && node scripts/check-overlaps.cjs auto-20260909-130105` — PASS: 10 контрольных кадров, 0 пересечений прямоугольников текста.
- Gap-скан режиссёра: 5 визуальных идей, после добавления нового визуала нет нерешённых gap `<=3`.

## Визуальный просмотр

Осмотрены вручную все 10 контрольных кадров на точках 35% и 75% сцен 0–4. Кадры вертикальные `1080×1920`, непустые; крупные титры, подписи IP:port, таблица отображений и CTA целиком помещаются в safe-зонах и читаются.

- Сцена 0: бытовой вход «несколько устройств — один внешний адрес» показан домом, роутером и сайтом.
- Сцена 1: частный `IP:порт` превращается в публичный `IP:порт`; перепись пакета и движение наружу понятны без технического образования.
- Сцена 2: аналогия гардероба не декоративная — номерок буквально ведёт к строке таблицы.
- Сцена 3: обратный lookup по публичному порту возвращает пакет конкретному телефону.
- Сцена 4: итог фиксирует `private IP:port → public IP:port`, поиск нужного телефона и важное ограничение «NAT/PAT не шифрует».

Новый визуал `nat-pat-translation` имеет четыре фазы (`home`, `translate`, `ticket`, `return`), поэтому механизм не сводится к неподвижной коробке с ярлыком. Переходные кадры не оставляют пустого экрана и не скрывают смысловые элементы. Плейсхолдеров, отладочного текста, дублирования строки narration на канвасе и повторяющихся сцен с одной картинкой не обнаружено.

В первом прогоне overlap-чек нашёл одно пересечение в сцене 1; режиссёр сдвинул подпись пакета, после чего повторный обязательный чек стал зелёным. Narration и episode JSON при исправлении не менялись.

Блокирующих замечаний нет.
