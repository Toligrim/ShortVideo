# Gap-скан: auto-20260910-082623

## 1. Путь и состав сцен

`episodes/auto-20260910-082623.json`: 6 сцен — story «Тормозишь — заряд есть» (`regenerative-braking`: `symptom` → `inverter`), story «Колодки теряют энергию» (`friction` → `title-slam`), story «Мотор меняет роль» (`generator` → `inverter`), story «Маленькая модель» (`dynamo` → `title-slam`), story «Когда подключаются колодки» (`blend` → `title-slam`), outro «Запомни».

Реплики всех шести блоков перенесены из draft дословно, включая разметку `{инвертор|инвертор}` и `{рекуперация|рекупера́ция}`. Драфтовые `slug`, `category`, `device` и `research` не являются полями Episode по схеме и в episode JSON не добавлялись.

### Раскадровка блоков

| Блок | Сущности → визуальные объекты | Действия → движение | Ударные слова → onWord |
|---|---|---|---|
| 1 | электромобиль, индикатор запаса хода, батарея | машина замедляется; запас возвращается; энергия идёт обратно | `Электромобиль`, `отправляет`, `батарею` |
| 2 | колодки, тормозной диск, тепло, воздух | колодки сжимают диск; диск нагревается; энергия рассеивается | `колодки`, `тепло`, `рассеивается`, `растёт` |
| 3 | колёса, тяговый мотор, обмотки, ток, инвертор, батарея | колёса крутят мотор; мотор становится генератором; ток возникает и проходит через инвертор в батарею | `колёса`, `мотор`, `генератором`, `обмотках`, `ток`, `инвертор`, `батарею` |
| 4 | велосипед, динамо, лампа, сопротивление вращению | колесо крутит динамо; лампа получает электричество; сопротивление замедляет велосипед | `велосипедное`, `динамо`, `электричество`, `сопротивление`, `замедляет` |
| 5 | энергия движения, батарея, колодки, полный/холодный аккумулятор, ток | часть потока возвращается; колодки изнашиваются медленнее; при ограничении тока подключаются колодки | `Часть`, `батарею`, `изнашиваются`, `полной`, `подключаются`, `нельзя` |
| 6 | колодки, мотор, инвертор, батарея, следующий бытовой симптом | движение превращается в тепло или электричество; инвертор возвращает энергию; появляется мостик к следующему ролику | native outro, битов/пакетов/state нет |

## 2. Gap-скан: 6 смысловых идей, 5 оценок ≤3

Группировка сделана по смысловым идеям, а не по отдельным глаголам:

| Идея из реплик | Лучший существующий visual | Соответствие 1–5 | Решение |
|---|---|---:|---|
| Электромобиль замедляется, а запас хода иногда возвращается; движение может пойти обратно в батарею | `battery-sei-growth` | 2 | В нём есть батарея и поток ионов, но нет машины, торможения и наблюдаемого роста запаса хода. В очередь кузницы. |
| Обычные колодки зажимают диск и рассеивают движение в тепло | `diagram` | 1 | Generic-узлы не показывают физический диск, колодки и нагрев. В очередь кузницы. |
| Тот же тяговый мотор становится генератором: ток возникает в обмотках, инвертор направляет его в батарею | `nfc-field-response` | 2 | Он показывает наведённый ток в катушке, но это NFC и другая физическая цепь: нет колеса, тягового мотора и силового инвертора. В очередь кузницы. |
| Велосипедное динамо одновременно даёт электричество и сопротивление вращению | `nfc-field-response` | 2 | Есть энергия и катушки, но нет динамо, лампы и механического сопротивления колесу. В очередь кузницы. |
| Часть энергии возвращается, колодки изнашиваются медленнее; при полной/холодной батарее и резком торможении колодки подключаются | `cold-battery-voltage-drop` | 2 | Он показывает холодную батарею и ограничение отдачи, но не смешение рекуперации с фрикционным торможением и не колодки. В очередь кузницы. |
| Итог: колодки дают тепло, мотор — электричество, инвертор возвращает его батарее; затем мостик к следующему ролику | `outro` | 5 | Нативные bullets/CTA outro подходят для итоговой выжимки; generic story-visual для этого не нужен. |

Пять оценок ≤3 объединены одной кузницей `regenerative-braking`, а не пятью одноразовыми компонентами. Он покрывает механическую причинную цепочку фазами `symptom`, `friction`, `generator`, `inverter`, `dynamo`, `blend`; generic `diagram` в episode не используется.

## 3. Новых визуалов: 1

`regenerative-braking` — 6 фаз, документирован в `.claude/skills/animator/catalog.md` в секции `## regenerative-braking`.

Спека и ограничения:

- `symptom`: машина + карточка запаса хода — 2 визуальных объекта.
- `friction`: диск/ступица + сжимающиеся колодки + тепловые волны — 3 объекта.
- `generator`: колесо + механическая связь + мотор с видимыми обмотками/зарядами — 3 объекта.
- `inverter`: мотор + силовой инвертор + батарея + направленный поток — 4 объекта.
- `dynamo`: велосипед с динамо + поток к лампе + лампа — 3 объекта.
- `blend`: батарея с условиями `100% · −10°C` + ограниченный поток + холодный узел колодок — 3 объекта.

Каждая фаза имеет только короткий заголовок визуала и один короткий badge-ярлык; подписи не копируют narration и укладываются в лимит двадцати символов. Смысл размещён между y≈338 и y≈1190, караоке остаётся в нижней зоне.

Право вето проверено:

- visual не является мега-компонентом: ровно 6 фаз, максимум 4 объекта в фазе;
- на границах соседних сцен фазы различаются: `inverter → friction → generator → dynamo → blend`; композиция меняется от машины и gauge к диску, мотору, велосипеду и батарее с тормозным узлом;
- повтор `inverter` находится в непоследовательных сценах и всё равно остаётся отдельной фазой от соседних кадров;
- кадры не заполнены капсулами или сплошными цветовыми заливками, цвета обозначают тепло, поток, ток и ограничение;
- пустых трёхсекундных кадров и дублирования строк narration на canvas нет;
- smoke/still нового visual был запущен для фаз `symptom`, `friction`, `generator`, `inverter`, `dynamo`, `blend`, но Read-просмотр не выполнен: Remotion не смог запустить доступный локальный headless-браузер в этом окружении. После сбоя типы, схема и валидатор проверены повторно; подробности приведены ниже.

### Проверки и ограничение окружения

- `python3 tools/validate.py episodes/auto-20260910-082623.json` — OK: 6 сцен, около 139 слов.
- `npx tsc --noEmit` в `video` — OK.
- smoke/still Remotion для всех шести фаз не создал PNG: скачивание Chrome Headless Shell запрещено сетевым окружением; локальные Chromium/headless-shell также завершились с ошибками sandbox/Crashpad (`Operation not permitted`). Поэтому Read-инструменту не было что просматривать; это остаётся ограничением сдачи, а не подтверждением визуального рендера.

## 4. onWord-якоря по сценам

1. Story «Тормозишь — заряд есть»: `Электромобиль` → `symptom`; `отправляет` → `inverter`.
2. Story «Колодки теряют энергию»: `колодки` → `friction`; `тепло` → `title-slam`.
3. Story «Мотор меняет роль»: `колёса` → `generator`; `инвертор` → `inverter`.
4. Story «Маленькая модель»: `велосипедное` → `dynamo`; `сопротивление` → `title-slam`.
5. Story «Когда подключаются колодки»: `Часть` → `blend`; `полной` → `title-slam`.
6. Outro «Запомни»: битов, пакетов и `state` нет, поэтому onWord-якорей нет.

Все onWord взяты из формы показа соответствующей реплики; порядок битов совпадает с порядком упоминания.
