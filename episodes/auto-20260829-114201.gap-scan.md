# Gap-scan: auto-20260829-114201

Тема: почему Face ID узнаёт тебя в полной темноте (TrueDepth, Flood Illuminator, VCSEL, карта глубины).

## Шаг 1 — раскадровка (сущности / действия / onWord)

- **Сцена 1 (hook):** «Полночь, телефон разблокировался во тьме — миф: «видит как кот»». Сущности: телефон, темнота, кот. Действие: противопоставление видимости в темноте. onWord: нет (хук, без битов).
- **Сцена 2:** «Face ID не видит во тьме. Он сам светит: {Flood Illuminator|флад иллюминатор}». Сущности: Face ID, Flood Illuminator. Действие: самосвет в темноте, невидимая подсветка. onWord: «светит», «тьме».
- **Сцена 3:** «{TrueDepth|тру-депт} проецирует тридцать тысяч невидимых точек». Сущности: TrueDepth, VCSEL, IR-камера, точки. Действие: проекция точек, построение карты глубины. onWord: «точек», «глубины».
- **Сцена 4:** «Точки падают на нос и скулы и искривляются. IR-камера читает это искажение». Сущности: точки, нос, скулы, IR-камера. Действие: искривление точек на рельефе, чтение искажения. onWord: «карту», «глубины».
- **Сцена 5:** «{Face ID|фей айди} не обманет фото — у фото нет глубины». Сущности: Face ID, фото. Действие: отсутствие глубины у фото, сравнение с 3D-рельефом. onWord: «глубины», «фото».
- **Сцена 6 (outro):** «Итог: Face ID работает в темноте, потому что сам светит невидимым светом». Сущности: Face ID, невидимый свет. Действие: итоговое утверждение.

## Шаг 2 — gap-скан (действие из реплики → лучший визуал → оценка 1–5)

| # | Действие из реплики | Визуал | Оценка |
|---|---|---|---|
| 1 | «Face ID не видит во тьме» (самостоятельный свет в темноте) | `face-id-depth` (фаза `darkness`) | 5 |
| 2 | «Он сам светит: {Flood Illuminator|флад иллюминатор}» (активация подсветки) | `face-id-depth` (фаза `darkness`) | 5 |
| 3 | «Проецирует тридцать тысяч невидимых точек» (IR- dots projection) | `face-id-depth` (фаза `dots`) | 5 |
| 4 | «{VCSEL|ви-си-эл} рисует рисунок» (создание точечного паттерна) | `face-id-depth` (фаза `dots`) | 5 |
| 5 | «IR-камера строит карту глубины» (построение карты) | `face-id-depth` (фаза `depth`) | 5 |
| 6 | «Точки падают на нос и скулы и искривляются» (искажение на рельефе) | `face-id-depth` (фаза `depth`) | 5 |
| 7 | «IR-камера читает это искажение» (анализ искажения) | `face-id-depth` (фаза `depth`) | 5 |
| 8 | «У фото нет глубины» (отсутствие 3D-данных) | `face-id-depth` (фаза `depth`) | 5 |
| 9 | «Face ID не обманет фото» (сравнение фото и 3D) | `face-id-depth` (фаза `depth`/сравнение) | 5 |

Итого действий: **9**. С оценкой ≤3: **0**.

Все 9 действий покрыты одним новым визуалом (`face-id-depth`), созданным для этого ролика. Оценки ≤3 отсутствуют — все механические действия имеют буквальный визуал с оценкой 5.

## Шаг 3 — новые визуалы

- **`face-id-depth`** — `video/src/scenes/FaceIdDepthVisual.tsx`; фазы `darkness` / `dots` / `depth`. Документация: `catalog.md` (новая секция `face-id-depth`).

Регистрации: `schema/scenes.schema.json` (enum добавлен), `video/src/lib/types.ts` (StoryBeat union), `video/src/scenes/StoryScene.tsx` (import, storySchedule, storySfx, render switch).

## Шаг 4 — onWord-якоря по сценам

- Сцена 2 («сам светит»): «светит» → активация Flood Illuminator
- Сцена 2 («во тьме»): «тьме» — установка сцены в полной темноте
- Сцена 3 («точки»): «точек» — проекция инфракрасных точек
- Сцена 3 («глубины»): «глубины» — построение карты расстояний
- Сцена 4 («карту»): «карту» — созданиеdepth-карты лица
- Сцена 4 («глубины»): «глубины» — итоговая карта глубины
- Сцена 5 («глубины»): «глубины» — у фото нет глубины
- Сцена 5 («фото»): «фото» — сравнение с плоской картинкой