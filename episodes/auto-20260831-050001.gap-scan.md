# Gap-скан: auto-20260831-050001 — «Почему биткоин нельзя просто скопировать?»

Дата: 2026-08-31
Драфт: episodes/drafts/auto-20260831-050001.draft.json (6 блоков, ~135 слов)

## Метод
Для каждой глагол/действие-фразы из реплики — лучший существующий визуал до кузницы (catalog v1 + digital-signature/block-chain) и оценка соответствия 1–5.
5 = буквально показывает, 3 = достроит зритель, 1 = про другое. Правило режиссёра: ≤3 → в кузницу или письменное обоснование.

| # | Действие из реплики | Лучший существующий визуал до кузницы | Оценка | Решение |
|---|---|---|---|---|
| 1 | «Скопировал — удвоил деньги» / копирование файла на флешке | `file-delete-recovery` (фаза unlink/overwrite — про удаление, не про копирование) / `password-leak` — про другое | 2 | **Новый `wallet-copy` phase `copy`** — literal: два файла wallet.dat (ноутбук→флешка, бейдж ×2 файла) против одной записи |
| 2 | «Деньги лежат на флешке» (миф про хранение) | `file-delete-recovery` | 2 | Тот же `wallet-copy:copy` (файл vs запись) |
| 3 | «{биткоин} — не файл у тебя, а запись в общей тетради сети» | `block-chain` phase `link` — четыре блока, заголовок «ОДНА ТЕТРАДЬ НА ВЕСЬ МИР», каждый блок зависит от prev-хэша | 5 | Оставляем `block-chain:link` — буквально |
| 4 | «Каждый перевод ты заверяешь {цифровая подпись} — штамп-печать с секретным ключом: штамп ставит только твой ключ» | `digital-signature` phase `sign` — приватный ключ + данные (сумма→получатель) → подпись σ | 5 | Оставляем `sign` |
| 5 | «а проверяет любой по отпечатку» (проверка открытым ключом) | `digital-signature` phase `verify` — публичный ключ + узлы проверки, бейдж «ПОДЛИННО» | 5 | Оставляем `verify` |
| 6 | «Внутри — математика эллиптической кривой» (кривая secp256k1) | `title-slam` — заголовок «secp256k1» / `digital-signature` не показывает кривую как геометрию | 3 | **Обоснование без нового визуала**: фраза — контекстный префикс (5 слов), ударение фразы дальше («Поменяешь…»). Отдельная визуализация кривой (точки на поле F_p) потребовала бы свой визуал на 2 сек и раздула бы кадр; `title-slam` + следующий буквальный `tamper` передают суть «математика ломает подмену» без потери правды. Оценка 3 зафиксирована, не маскируется. |
| 7 | «Поменяешь хоть букву в сумме — подпись становится недействительной» | `digital-signature` phase `tamper` — слева «БЫЛО ПОДПИСАНО 0,5 BTC→...», справа перечёркнуто и подсвечена подмена 5,0 BTC + бейдж «ПРОВЕРКА РУХНУЛА» | 5 | Оставляем `tamper` |
| 8 | «тысячи узлов отбрасывают такой перевод» | `digital-signature:tamper` (подпись рушится → сеть отбрасывает) / `block-chain:tamper` (цепочка выше рассыпается) | 4 | Покрывается тем же `tamper` (подпись σ не совпадает → «транзакция отвергнута» + тысячи узлов в `wallet-copy:double` для усиления) |
| 9 | «копирование файла кошелька не копирует деньги» (повтор мифа) | `wallet-copy:copy` (если бы существовал) иначе 1 | 2 → после кузницы 5 | **Новый `wallet-copy:copy`** |
| 10 | «попробуешь потратить один и тот же выход дважды» (попытка double-spend) | `mempool-rbf` (про замену в мемпуле) — не UTXO, `block-chain:tamper` — про переписывание истории | 2 | **Новый `wallet-copy` phase `double`**: один UTXO #a3f1 и две транзакции из него (показывает единый выход vs две попытки) |
| 11 | «сеть примет первый, второй отвергнет» (консенсус по первому) | `wallet-copy:double` (после кузницы 5) / без него `block-chain:confirm` — про глубину, не про отвержение второго | 2 → после 5 | Тот же `wallet-copy:double` — зелёная «ПРИНЯТА» vs красная «ОТВЕРГНУТА» + бейдж «ТЫСЯЧИ УЗЛОВ» буквально совпадает с «тысячи узлов отбрасывают» |
| 12 | «право тратить, доказанное цифровой подписью. Без ключа подпись не подделать» (итог) | `digital-signature:sign/verify` — обобщение | 4 | Покрывается `outro` + уже показанными sig-визуалами; отдельного нового визуала не требует |

## Итог
- Действий проанализировано: 12
- С оценкой ≤3 до кузницы: **5** (№1, 2, 6, 9, 10, 11 — фактически 5 уникальных действий: копирование файла ×2, двойная трата ×2, кривая ×1)
- После кузницы: копии и double-spend поднялись до 5 благодаря `wallet-copy`; единственная оставшаяся ≤3 — №6 эллиптическая кривая (оценка 3, с письменным техническим обоснованием выше, не замаскирована).
- Новый визуал создан: **1** — `wallet-copy` (phases `copy`/`double`) в `video/src/scenes/StoryScene.tsx`, зарегистрирован в `video/src/lib/types.ts`, `schema/scenes.schema.json`, `video/src/scenes/StoryScene.tsx` (schedule/sfx/cams/switch), задокументирован в `.claude/skills/animator/catalog.md`.
- Техпроверка: `cd video && npx tsc --noEmit` ✓, `npx remotion still Preview --props '{"scene":{"type":"story","beats":[{"visual":"wallet-copy","params":{"phase":"copy"}}]}}'` ✓ (1009K png), вторая фаза `double` ✓.

## Раскадровка → план сцен
Блок 0 → hook (интрига, без бита); Блок 1 → story: wallet-copy:copy + block-chain:link; Блок 2 → story: digital-signature:sign + verify; Блок 3 → story: title-slam secp256k1 + digital-signature:tamper; Блок 4 → story: wallet-copy:copy + wallet-copy:double; Блок 5 → outro.
