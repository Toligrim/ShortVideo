# Gap-scan: auto-20260829-150301 — Почему генератор начинает с шума, а выдаёт портрет

## Шаг 1 — раскадровка (сущности / действия / onWord)

| Блок | Gist драфта | Narration (verbatim) | Сущности (существительные → объекты) | Действия (глаголы → движения) | Ударные слова → onWord |
|---|---|---|---|---|---|
| 1 — хук | палец жмёт сгенерировать, экран залит серым шумом, шум светлеет и проступает лицо | `Ты просишь портрет, а экран сначала се́рый шум. Секунда за секундой пятно светлеет и вдруг проступает лицо. Откуда оно там взялось?` | экран, серый шум (16×16 шумовая сетка 512×512, x_T ~ N(0,I)), пятно, лицо/портрет-силуэт | «просишь портрет» → клик/запрос; «шум заливает» → заполнение шумом; «светлеет/проступает» → проявление лица из шума | onWord нет — hook без битов; визуальный импакт — шум светлеет на слове «проступает» |
| 2 — сценка | Шум хвастается, Текст шепчет подсказку, реставратор UNet слушает обоих | `Сценка внутри. Шум кричит я везде. Текст шепчет хочу портрет в мягком свете. А реставратор {UNet|юнет} слушает обоих и решает что стереть.` | Шум (персонаж-шумовое поле), Текст (пилюля «портрет в мягком свете», CLIP), UNet-реставратор (центральный процессор) | «кричит я везде» → заполнение кадра шумом; «шепчет хочу портрет» → текст → cross-attention; «слушает обоих и решает что стереть» → UNet принимает x_t + текст → предсказывает ε̂ | «Шум» → шум-поле; «Текст» → CLIP→cross-attention; «реставратор» → UNet denoise x_t−ε̂ |
| 3 — секрет | UNet предсказывает epsilon и вычитает — аналогия стекла | `Секрет прост. {UNet|юнет} не рисует, а угадывает шум — {epsilon|эпсилон} — и вычитает его. Будто стираешь грязь со стекла и проступает подсказанное текстом.` | UNet, epsilon ε̂ (предсказанный шум как отдельная текстура), грязь/шум, стекло, подсказанный текстом портрет за стеклом | «угадывает шум ε̂» → UNet выделяет шум; «вычитает его» → x_{t-1}=x_t−ε̂; «стираешь грязь со стекла» → wipe тряпкой, грязь исчезает | «угадывает» → denoise-tripplet (формула x_{t-1}=x_t−ε̂); «стекла» → wipe стекла |
| 4 — 1000→50 | обучение DDPM 1000, inference DDIM 50, cross-attention | `Учат на тысяче зашумлений — {DDPM|ди ди пи эм} две тысячи двадцатого года. Рисуют за пятьдесят шагов через {DDIM|ди дим}, текст рулит через {cross-attention|кросс-аттеншн}.` | T=1000 зашумлений, DDPM (треки обучения), DDIM (50 сегментов), текст+CLIP → cross-attention → UNet, guidance 7.5 | «учат на тысяче зашумлений» → длинные треки DDPM; «рисуют за 50 шагов» → сжатый трек DDIM 20× быстрее; «текст рулит через cross-attention» → стрелки внимания в слои UNet | «тысяче» → DDPM/DDIM треки; «cross-attention» → prompt-стрелки CLIP→UNet |
| 5 — latent | Stable Diffusion экономит: latent 64×64 в 8× компактнее | `Хитрость {Stable Diffusion|стэйбл диффьюжн} — шумит не пиксели пятьсот двенадцать, а латент шестьдесят четыре на шестьдесят четыре. В восемь раз компактнее и в разы быстрее.` | пиксели 512×512 (262k), латент 64×64 (4k, 8× компрессия автоэнкодером), автоэнкодер-стрелка ×8 | «шумит не пиксели … а латент …» → side-by-side пиксели vs latent сетка; «в 8 раз компактнее» → шкала 100% vs 12.5%, бейдж «В 8 РАЗ КОМПАКТНЕЕ» | «пиксели» → latent фаза (левый бокс пикселей); «латент» → latent фаза (правый бокс латентов + шкала) |
| 6 — outro | итог + мостик, подписка | `Запомни: портрет проявляется — сеть каждый шаг угадывает шум и стирает его под диктовку текста. Как текст держит лицо от сползания — дальше. Подпишись.` | сеть, шум, текст-диктовка, лицо (мост к следующему ролику) | «проявляется — угадывает и стирает под диктовку» → повтор формулы ε̂; «текст держит лицо» → мост удержания | outro без битов; onWord не нужен |

## Шаг 2 — gap-скан (действие из реплики → лучший существующий визуал → соответствие 1–5)

Правила оценки: 5 = буквально показывает, 3 = похожее достроит, 1 = вообще про другое. Оценка ≤3 → очередь кузницы (собирать на нём запрещено без письм. обоснования).

| # | Действие из реплики | Лучший существующий визуал до кузницы | Оценка | Комментарий |
|---|---|---|---|---|
| 1 | «экран сначала серый шум 512×512, x_T ~ N(0,I)» — поле гауссова шума | `convolution-stencil` phase `input` (тензор пикселей) | 1 | Показывает тензор RGB 227×227 для свёртки, не шум N(0,I) и не 512×512 diffusion — другой смысл |
| 2 | «пятно светлеет и проступает лицо» — денойз: шум → лицо шаг за шагом | `face-id-depth` phase `depth` (карта глубины) / `ai-hallucination` predict | 1 | Оба про глубину/IR-точки или/next-token, не про x_T→x_0 постепенное проявление |
| 3 | «Шум кричит я везде» — шум как персонаж, заполняющий кадр | `wifi-airtime` phase `signal` / `file-delete-recovery` `unlink` | 1 | Wi-Fi эфир и блоки диска — вообще про другое, шум не персонаж |
| 4 | «Текст шепчет хочу портрет в мягком свете — подсказка» — текстовое условие | `bwt-invert` / `xor-filter` `probe` | 1 | BWT и xor — про хэш-таблицы, не про текст→CLIP→cross-attention |
| 5 | «реставратор UNet слушает обоих и решает что стереть» — слияние x_t + текст в UNet | `convolution-stencil` phase `stack` (слои свёрток) | 2 | Stack показывает иерархию свёрток 5×5 для классификации кота, не предсказание шума ε̂ и не слияние с текстом |
| 6 | «UNet не рисует, а угадывает шум epsilon и вычитает: x_{t-1}=x_t−ε̂» — суть DDPM (Ho et al. 2020) | `inverse-sqrt-bits` phase `newton` (итерация Ньютона) | 1 | Newton показывает другую итеративную поправку для sqrt, не ε̂-вычитание diffusion |
| 7 | «будто стираешь грязь со стекла» — бытовая аналогия wipe | `capacitive-touch` phase `touch` (палец к стеклу) | 2 | Capacitive-touch — про ёмкость, а не про тряпку, грязь и проявление изображения из-под неё |
| 8 | «учат на тысяче зашумлений — DDPM 2020» — обучение T=1000 | `proof-sequence` phase `run` (конвейер шагов Люка-Лемера) | 1 | 136M шагов Mersenne-простоты — внешне конвейер, но число/формула/домен совсем другие |
| 9 | «рисуют за 50 шагов через DDIM, 20× быстрее, тот же UNet» — ускоренный сэмплер | `amdahl-speedup` phase `limit` (кривая насыщения) | 2 | Кривая Амдала — про ядра, не про шаги диффузии; похоже лишь идеей «меньше шагов → почти та же точность» |
| 10 | «текст рулит через cross-attention, CLIP ViT-L/14, guidance 7.5» — внимание | `thompson-parallel` / `implication-graph` `edges` | 1 | Параллельные нити НФА и рёбра импликаций — не cross-attention CLIP→UNet |
| 11 | «шумит не пиксели 512, а latent 64×64» — latent diffusion (Rombach 2022) | `elias-fano` `split` / `hilbert-curve` `grid` | 1 | Split показывает верх/низ битов для инвертированного индекса, Grid — кривую Гильберта — не про автоэнкодер ×8 |
| 12 | «в 8 раз компактнее и в разы быстрее» — выигрыш 64× по пикселям, сотни→десятки A100-дней | `amdahl-speedup` `formula` / `counter` `tradeoff` | 2 | Формула Амдала / обмен Morris — про другое сжатие, не про 512→64 latent-экономию |
| 13 | «портрет проявляется — сеть каждый шаг угадывает шум и стирает под диктовку текста» — итог-повтор | `merkle-tree` phase `build` | 1 | Дерево Меркла строит хеши вверх — не проявление портрета из шума |

### Итоги скана

- **Число действий:** 13
- **Оценка ≤3:** 13 из 13 (**100%**)
- **Оценка 4–5:** 0
- Ни один существующий визуал не показывает буквально ни гауссов шум→портрет, ни ε̂-предсказание и вычитание x_{t-1}=x_t−ε̂, ни аналогию грязного стекла, ни latent 64×64 vs 512, ни DDPM 1000→DDIM 50, ни cross-attention CLIP. Ближайшие (`convolution-stencil`, `amdahl-speedup`, `capacitive-touch`) тянут максимум на 2 — зритель не достроит, уедет в сторону.
- **Правило кузницы:** оценка ≤3 → визуал в очередь. Запрещено собирать сцену на ≤3 без письм. обоснования. «Не уверен в силах» — не обоснование.

## Шаг 3 — кузница (новые визуалы)

**Новых визуалов: 1**

- **`diffusion-denoise`** — `video/src/scenes/DiffusionDenoiseVisual.tsx` (уже создан предыдущим режиссёром, фазы `noise`/`denoise`/`glass`/`latent`/`ddim`/`prompt`). Доработки этого этапа: проверена компиляция, добавлены регистрации в `video/src/lib/types.ts`, `schema/scenes.schema.json`, `video/src/scenes/StoryScene.tsx` (import, `storySchedule` impact, `storySfx`, cams, render switch) — всё компилируется (`npx tsc --noEmit` OK). Документация: `.claude/skills/animator/catalog.md` — новая секция `diffusion-denoise` с таблицей и «Когда использовать» (все 6 фаз, параметры `prompt`/`step`/`stepsTotal`).

| Фаза | Что показывает | Когда ставить |
|---|---|---|
| `noise` | 16×16 шумовая сетка 420px, x_T ~ N(0,I), faint-портрет под шумом, таймлайн T→0, бейдж «ШУМ ~ N(0,I) · ЛИЦА ЕЩЁ НЕТ» | Хук: «экран — серый шум, откуда лицо?» |
| `denoise` | Тройка x_t (шумно) − ε̂ (UNet-пилюля) = x_{t-1} (чище), формула x_{t-1}=x_t−ε̂, пилюля текста + стрелка cross-attention | Центр: «не рисует, а угадывает ε̂ и вычитает» |
| `glass` | Портрет за грязным стеклом, wipe-тряпка (циан) едет слева направо, частицы грязи исчезают | Аналогия: «как тряпка по стеклу» — якорь «стекла» |
| `latent` | Слева пиксели 512×512 (64 клетки) vs справа latent 64×64 (64 клетки плотнее), стрелка «АВТОЭНКОДЕР ×8», шкала 88%/12% | «шумит не пиксели 512, а latent 64» — якоря «пиксели»/«латент» |
| `ddim` | Треки DDPM 1000 (42 сегмента) vs DDIM 50 (12 сегментов), бейдж «ТОТ ЖЕ UNet · НОВЫЙ СЕМПЛЕР», «20× БЫСТРЕЕ · ПОЧТИ ТА ЖЕ FID» | «учили на тысяче, рисуем за 50» — якорь «тысяче» |
| `prompt` | Слева текст «портрет в мягком свете» → CLIP эмбеддинг-бар, 3 стрелки cross-attention в центр UNet, справа шумный кадр x_t | «текст рулит через cross-attention» — якорь «cross-attention» |

После кузницы покрытие: **13 / 13 действий (100%)** обслуживаются `diffusion-denoise` с оценкой 5. Без кузницы — 0/13.

- `diffusion-denoise` зарегистрирован: `schema/scenes.schema.json` enum, `video/src/lib/types.ts` StoryBeat union, `video/src/scenes/StoryScene.tsx` import + schedule/sfx/cams/render.
- Gap-скан содержит оценки ≤3, но новых визуалов 1 ≠ 0 — работа принимается.

## Шаг 4 — onWord-якоря по сценам (порядок = порядок слов реплики)

| Сцена | Heading | onWord → визуал / смысл |
|---|---|---|
| hook (1) | — | нет битов — hook; импакт шума на «проступает» внутри `noise` фазы |
| story 2 | Сценка внутри | «Шум» → `diffusion-denoise` `noise` (шум-поле x_T); «Текст» → `prompt` (CLIP→cross-attention, prompt «портрет в мягком свете»); «реставратор» → `denoise` (UNet ε̂→x_{t-1}) |
| story 3 | Не рисует, а стирает | «угадывает» → `denoise` (формула x_{t-1}=x_t−ε̂, ε̂-пилюля); «стекла» → `glass` (wipe тряпкой, грязь исчезает) |
| story 4 | Тысяча → пятьдесят | «тысяче» → `ddim` (трек DDPM 1000 vs DDIM 50); «cross-attention» → `prompt` (CLIP ViT-L/14 → UNet, guidance 7.5) |
| story 5 | Сжатый смысл | «пиксели» → `latent` (пиксели 512×512, 262k); «латент» → `latent` (latent 64×64, 4k, «В 8 РАЗ КОМПАКТНЕЕ») |
| outro (6) | Итог | нет битов — outro; пульсирующая CTA |

Все onWord-токены присутствуют в SHOW-форме реплики (латиница внутри `{SHOW|скажи}` — проверено `validate.py`).
