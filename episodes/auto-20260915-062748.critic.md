# Критика: auto-20260915-062748

## Вердикт

**ПРИНЯТО.**

После одного critic-round исправлен baseline сцены 2: в фазе
`envelopes/pair` короткий и подробный конверты, а также `text/plain` /
`text/html`, видимы до cue `short`/`rich` с opacity 0.42. Transfer и
реакции на слова сохранены.

## Проверки

- `python3 tools/validate.py episodes/auto-20260915-062748.json` — OK:
  5 сцен, около 135 слов.
- `npx tsc --noEmit` — exit 0.
- `node video/scripts/check-motion.cjs ...` — exit 0: 1944 кадров,
  24 cue-triplets, boundaries построены.
- `node video/scripts/check-overlaps.cjs auto-20260915-062748` — exit 0:
  0 overlaps во всех 10 DOM-контрольных кадрах.
- Визуально просмотрены 10 кадров 35%/75% всех сцен; после правки отдельно
  просмотрен `scene 2a`, кадр 819.
- Использовано: 1 overlap-round и 1 critic-round из разрешённых бюджетов.

## Что уносит зритель

Одна операция копирования кладёт один фрагмент в несколько представлений
`text/plain`, `text/html` и при необходимости `text/rtf`, а приложение
выбирает самое богатое из понятных ему — поэтому Word сохраняет оформление, а
простой получатель оставляет буквы.
