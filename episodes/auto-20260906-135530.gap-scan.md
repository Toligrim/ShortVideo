# Gap-скан: `auto-20260906-135530`

## Раскадровка по блокам драфта

Реплики ниже приведены дословно; якоря `onWord` взяты из самих реплик и идут слева направо.

| блок | сущности | действия из реплики | onWord-якоря и визуальная роль |
|---|---|---|---|
| 1 | ты; ИИ; дата-центры; тысячи видеокарт; ответ; очередь | строят дата-центры; убирают GPU; ждут ответ; очередь растёт | `Ты` → `hall`: зал и стойки; `тысячами` → `scale`: повторяющийся парк; `Убери` → `queue`: ответ есть, но ожидание растёт |
| 2 | главный счёт; матрицы A/B/C; тысячи работников; числа; строка; столбец; результат | перемножают таблицы; одновременно выбирают числа; складывают попарные произведения в ячейку | `Внутри` → `tables`; `тысячи` → `workers`; `Каждое` → `dot-product` |
| 3 | большая модель; один ответ; умножения; следующий кусочек текста | выполняют миллионы однотипных шагов; повторяют их снова и снова; считают следующий кусочек | `миллионов` → `repeat`; `снова` → повторный бит `repeat` |
| 4 | видеокарта; одинаковые операции; одна карта; несколько карт; части модели; запросы | выполняют операции одновременно; ускоряют расчёт; держат части модели; обслуживают запросы | `Видеокарта` → `parallel`; `несколько` → `shards` |
| 5 | видеокарты; картинка; огромные таблицы; модель; люди; карты; следующий ролик; подписчик | отделяют GPU-вычисления от графики; быстро перемножают таблицы; масштабируют парк по модели и спросу; анонсируют продолжение | outro без битов: текст и CTA связывают матрицы, масштаб и следующий ролик |

## Полный gap-скан до кузницы

Шкала: 5 — буквальное совпадение действия и объекта; 3 — общий механизм угадывается, но важная деталь отсутствует; 1 — визуал о другом. Оценка `≤3` отправляет действие в очередь кузницы и ниже получает письменное решение.

| # | действие из реплики | лучший существующий визуал | соответствие | объяснение |
|---:|---|---|---:|---|
| 1 | строят дата-центры с тысячами видеокарт | `diagram` | 2 | узлы и линии не дают физического серверного зала, стоек или GPU-парка; очередь `gpu-data-center: hall/scale` |
| 2 | убрать видеокарты | `file-delete-recovery` | 2 | перечёркнутый файл — не исчезнувший вычислительный парк; очередь `gpu-data-center: queue` |
| 3 | ждать ответ слишком долго | `mail-queue` | 3 | ожидание видно, но это очередь писем, а не inference без ускорителя; очередь `gpu-data-center: queue` |
| 4 | очередь растёт | `mail-queue` | 3 | форма очереди подходит, контекст запросов к модели и готового ответа отсутствует; очередь `gpu-data-center: queue` |
| 5 | матричное умножение | `convolution-stencil: scan` | 3 | есть локальные умножение и сумма, но нет двух таблиц A/B и результата C; очередь `matrix-multiply: tables` |
| 6 | тысячи работников берут числа из двух таблиц | `amdahl-speedup: workers` | 3 | показывает параллельных работников, но не таблицы и выбор ячеек; очередь `matrix-multiply: workers` |
| 7 | работники делают это одновременно | `amdahl-speedup: workers` | 4 | параллельные дорожки и работники читаются буквально; существующего визуала достаточно |
| 8 | каждое число результата — сумма попарных произведений | `convolution-stencil: scan` | 3 | формула похожа, но вычисляется отклик свёртки, а не ячейка матрицы; очередь `matrix-multiply: dot-product` |
| 9 | брать числа из строки одной таблицы и столбца другой | `convolution-stencil: scan` | 2 | окно фильтра не объясняет пересечение строки A и столбца B; очередь `matrix-multiply: dot-product` |
| 10 | один ответ требует миллионов умножений | `convolution-stencil: input` | 3 | крупный счётчик есть, но он про пиксели CNN, не про ответ большой модели; очередь `matrix-multiply: repeat` |
| 11 | модель повторяет умножения | `attention-cost: quadratic` | 2 | пары и кривая сложности не показывают повторяющийся цикл умножений; очередь `matrix-multiply: repeat` |
| 12 | считать следующий кусочек текста | `context-window: tokens` | 3 | токены видны, но переход «счёт → следующий кусочек» не виден; очередь `matrix-multiply: repeat` |
| 13 | видеокарта выполняет множество одинаковых операций | `amdahl-speedup: workers` | 3 | параллельность есть, но нет GPU и сетки одинаковых `×`/`+`; очередь `gpu-data-center: parallel` |
| 14 | выполнять множество операций одновременно | `amdahl-speedup: workers` | 4 | независимые параллельные дорожки прямо передают «одновременно»; существующий visual можно оставить |
| 15 | одна видеокарта ускоряет расчёт | `amdahl-speedup: workers` | 4 | сравнение одного и многих потоков передаёт ускорение одной карты достаточно ясно |
| 16 | несколько карт держат части большой модели | `amdahl-speedup: workers` | 2 | нет ни карт, ни разрезанной модели; очередь `gpu-data-center: shards` |
| 17 | карты обслуживают больше запросов | `diagram` | 2 | обмен узлов не показывает несколько входящих запросов и capacity inference; очередь `gpu-data-center: shards` |
| 18 | видеокарты нужны не для картинки | `outro` | 4 | это итоговая граница, которую можно явно закрепить текстом outro; отдельная механика здесь не требуется |
| 19 | быстро перемножают огромные таблицы чисел | `convolution-stencil: scan` | 3 | суммирование похоже, но объектом остаётся фотофильтр; очередь `matrix-multiply: tables/dot-product` |
| 20 | чем больше модель и людей спрашивает, тем больше карт нужно | `amdahl-speedup: limit` | 3 | показывает насыщение параллелизма, но не связывает размер модели, спрос и физические карты; очередь `gpu-data-center: scale/shards` |
| 21 | дальше разобрать задержку ИИ и подписаться | `outro` | 5 | это прямое назначение финального CTA, не текущий вычислительный механизм |

**Итого: 21 действие, оценок `≤3`: 16.**

## Очередь кузницы и решения

Низкие оценки не оставлены без обоснования: восемь действий закрывает новый `gpu-data-center`, восемь — новый `matrix-multiply`. Оставленные оценки 4–5 используют существующий visual, потому что ключевое действие уже читается без подмены объекта.

| новый visual | закрытые действия | почему это буквальное решение |
|---|---|---|
| `gpu-data-center` | 1–4, 13, 16–17, 20 | реальные стойки и повторяющиеся GPU дают наблюдаемый дата-центр; `queue` физически убирает парк и оставляет ожидание; `parallel` ставит один GPU рядом с одинаковыми операциями; `shards` раскладывает модель по картам и добавляет запросы |
| `matrix-multiply` | 5–6, 8–12, 19 | `tables` показывает A/B/C; `workers` назначает ячейкам независимых работников; `dot-product` буквально сводит строку и столбец в `1×2 + 2×1 + 0×3 = 4`; `repeat` связывает `1 000 000+`, одинаковые `×`/`+` и следующий кусочек текста |

## Проверка причинной дуги

Порядок сцен ведёт от наблюдаемого входа к внутреннему механизму и следствию: зал и парк GPU → две таблицы и одна вычисленная ячейка → повторение до следующего текста → параллельность, шардирование и запросы → короткий вывод с CTA. В episode JSON используются только новые визуалы для пунктов очереди кузницы, а narration всех пяти блоков перенесён дословно.
