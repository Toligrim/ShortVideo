# Gap-Scan: auto-20260823-122702 (счётчик Морриса)

Скан по драфту `episodes/drafts/auto-20260823-122702.draft.json`: действие из реплики →
лучший существующий (на момент скана) визуал → соответствие 1–5.
Правило: оценка ≤3 → визуал в очередь кузницы.

## Хук (блок 1) — сцена `hook`, битов нет

Хук показывает только title/subtitle, битовые визуалы не нужны. Действия блока
(«точный растёт», «сосед — один байт», «до ста тридцати тысяч», «врёт честно»)
уходят в заголовок и подзаголовок — вне скана.

## Сцена 2 (блок 2): «плюс один — только иногда»

| Действие из реплики | Лучший существующий | Оценка |
|---|---|---|
| Точный: каждое событие — плюс один | — (нет счётного визуала) | 1 |
| Моррис: прибавляю единицу только иногда | `coin-pair` | 3 |
| Чем выше число, тем реже монетка | — | 1 |
| Вероятность шага = 2^(−уровень) | — | 1 |

→ **NEW `counter`**: фазы `exact` / `flip` / `decay` / `formula`. Итог 5/5/5/5.

## Сцена 3 (блок 3): «внутри — логарифм»

| Действие из реплики | Лучший существующий | Оценка |
|---|---|---|
| Хранится не количество, а его логарифм | — | 1 |
| Восьмибитный регистр до 130 000 событий | `hash-table` (8 ячеек) | 3 |
| Относительная ошибка ≤ 24% в 95 из 100 | `verdict-scan` | 2 |

→ **NEW `counter`**: фазы `logarithm` / `accuracy`. Итог 5/4/5.

## Сцена 4 (блок 4): «точность за память»

| Действие из реплики | Лучший существующий | Оценка |
|---|---|---|
| «Для бухгалтерии ты бесполезен» (обвинение) | `title-slam` | 4 |
| Датчику важнее заметить рост потока | `diagram` | 3 |
| Маленькая ошибка экономит память | — | 1 |

→ Обвинение оставлено на `title-slam` (4). Остальное — **NEW `counter`**,
фаза `tradeoff`. Итог 5/5.

## Аутро (блок 5)

Сцена `outro` (bullets + cta) — битовых визуалов не требует. Вне скана.

## Итог

- Просканировано действий: **10** (по story-сценам; хук/аутро — свои типы сцен).
- До кузницы: **8 из 10** с оценкой ≤3.
- Новых визуалов: **1** — `counter`
  (`exact` / `flip` / `decay` / `formula` / `logarithm` / `accuracy` / `tradeoff`),
  задокументирован в `.claude/skills/animator/catalog.md`.
- После кузницы: **0** действий с оценкой ≤3; один готовый визуал выше 3
  (`title-slam`, оценка 4) использован как есть.
