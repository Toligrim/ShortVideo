# Шаблон записи — corrections/<slug>/REPORT.md

Скопируй этот файл в `corrections/<slug>/REPORT.md` и заполни. Если эпизод
правится не в первый раз — не создавай новый файл, добавь ещё один раздел
«## Правка N (дата)» в конец существующего REPORT.md.

Правила заполнения и что сюда входит — см. `corrections/CLAUDE.md`.

---

## Правка 1 (ГГГГ-ММ-ДД)

### Эпизод
- slug:
- тема/заголовок:
- дата прогона продюсера:
- run_id (`runs/<run_id>/manifest.json`):
- ссылка на видео/скриншот проблемы, если сохранился:

### Что сломалось
Список конкретных дефектов, каждый отдельным пунктом. По возможности —
дословно, что увидел/услышал пользователь, а не пересказ своими словами.

### Как нашли
Кто/что заметило первым: пользователь визуально/на слух, автоматическая
проверка (какая именно — overlap-gate, STT-сверка, критик, gap-скан...),
или что-то ещё. Если инструмент — укажи команду, которой воспроизвести.

### Почему сломалось (root cause)
Не «опечатка» и не «модель ошиблась» — реальный механизм: чей это был шаг
конвейера (сценарист/режиссёр/TTS/рендер/оркестратор/делегирование), что
именно в промпте/коде/сценарии делегирования пошло не так, разовая
случайность это или системный риск, который повторится. Если разбирался
лог прогона (`runs/<run_id>/cli-stderr.log` и т.п.) — процитируй ключевые
места с номером строки/времени.

### Что исправили в ролике
Конкретные правки контента: что было → что стало. Для текстовых правок —
цитатами.

### Что исправили в конвейере
Файлы, инструменты, промпты, skill'ы — что изменилось системно, чтобы класс
ошибки не повторился (а не только этот конкретный ролик). Список
коммитов — хэш + однострочное сообщение.

### Урок / класс ошибки
Единичный случай или представитель более широкого класса? Что стоит
проверять/иметь в виду при следующих похожих инцидентах — вплоть до
конкретных мест в коде, которые ещё не защищены, но по той же логике могут
сломаться так же.
